<!DOCTYPE html>
<html lang="en">
<head>
<meta charset="UTF-8" />
<title>Add Inventory</title>
<script>
    function validateForm() {
        // Add specific validations for each field if needed
        return true;
    }
</script>
</head>
<body>
    <h1>Add Vehicle to Inventory</h1>

    <% if (message) { %>
    <div class="flash"><%= message %></div>
    <% } %>
    
    <% if (errors && errors.length > 0) { %>
    <ul class="errors">
        <% errors.forEach(err => { %>
        <li><%= err.msg %></li>
        <% }) %>
    </ul>
    <% } %>
    
    <form action="/inv/add-inventory" method="post" onsubmit="return validateForm()">
    <label for="make">Make</label>
    <input type="text" id="make" name="make" value="<%= inventory?.make || '' %>" required>

    <label for="model">Model</label>
    <input type="text" id="model" name="model" value="<%= inventory?.model || '' %>" required>

    <label for="year">Year</label>
    <input type="number" id="year" name="year" value="<%= inventory?.year || '' %>" required>

    <label for="price">Price</label>
    <input type="number" id="price" name="price" value="<%= inventory?.price || '' %>" required>

    <label for="description">Description</label>
    <textarea id="description" name="description" required><%= inventory?.description || '' %></textarea>

    <label for="image">Image Path</label>
    <input type="text" id="image" name="image" value="<%= inventory?.image || '/images/vehicles/no-image.png' %>" required>

    <label for="thumbnail">Thumbnail Path</label>
    <input type="text" id="thumbnail" name="thumbnail" value="<%= inventory?.thumbnail || '/images/vehicles/no-image-tn.png' %>" required>

    <label for="classification_id">Classification</label>
    <%- classificationList %>

    <button type="submit">Add Vehicle</button>
</form>
</body>
</html>
<!-- This code is for adding a new vehicle to the inventory. It includes a form with fields for make, model, year, price, description, image path, thumbnail path, and classification. The form submits to the server for processing. -->
<!-- The page also displays any messages or errors that occur during the process. -->
